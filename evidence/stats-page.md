## HAProxy Stats Page

- Verified authenticated access to the HAProxy stats endpoint.
- Backend servers correctly report UP/DOWN state when Nginx is stopped or started.
- Access is restricted at the network level using AWS Security Groups.

